# add role to lambda for work with db
- ${file(resources/lambda-service-role.yml)}

# Export api gateway base url
#- ${file(resources/base-url-export.yml)}
#- ${file(resources/apiGateway-export.yml)}
#- ${file(resources/db.yml)}
